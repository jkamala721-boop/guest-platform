package com.guest_platform.entity;

/** Host-recipient events; deliberately separate from guest NotificationType. */
public enum HostNotificationType {
    PAYMENT_CONFIRMED,
    PAYOUT_ISSUE,
    BOOKING_CANCELLED
}

