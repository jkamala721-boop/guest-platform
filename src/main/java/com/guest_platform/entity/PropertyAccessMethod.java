package com.guest_platform.entity;

/** The host-selected way a confirmed guest accesses a property. */
public enum PropertyAccessMethod {
    KEY, LOCKBOX, SMART_LOCK, RECEPTION, HOST_HANDOFF, OTHER
}
